<div>
    <!-- I have not failed. I've just found 10,000 ways that won't work. - Thomas Edison -->
    <div class="row mb-3">
    <label for="{{$label}}" class="col-sm-2 col-form-label">{{$name}}</label>
    <div class="col-sm-10">
      <input type="{{$type}}" class="form-control"name="{{$name}}" value="{{old("Email")}}"> 
    </div>
  </div>
</div>